# Chess lab

Just chess bot.

---

```Shell

chmod u+x build.sh

# Скомпилирует со стандартным именем в директорию build
./build.sh

# Скомпилирует с заданным именем
BIN_NAME="testname" ./build.sh
```
